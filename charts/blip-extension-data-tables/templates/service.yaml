apiVersion: v1
kind: Service
metadata:
  name: {{ template "blip-extension-data-tables.fullname" . }}-{{ .Values.environment.stage }}
  labels:
    app: {{ template "blip-extension-data-tables.name" . }}
    chart: {{ template "blip-extension-data-tables.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "blip-extension-data-tables.name" . }}
    release: {{ .Release.Name }}